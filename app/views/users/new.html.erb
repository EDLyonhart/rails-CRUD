<h1>Add a user</h1>

<% if @user.errors.any? %>
  <% @user.errors.full_message.each do |error| %>
    <p>
      <%= error %>
    </p>
  <% end %>
<% end %>

<%= form_for @user, url:"/users" do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>